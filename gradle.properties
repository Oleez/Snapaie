org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Optional: SHA-256 of official .litertlm files — verify before production release (see README).
snapaie.model.sha256.e2b=
snapaie.model.sha256.e4b=
# Override in Play Console / server config; placeholders for local testing:
snapaie.billing.subscription.id=snapaie_pro_monthly
snapaie.billing.lifetime.id=snapaie_pro_lifetime
# Sample AdMob app id (matches Google test IDs when using banner test unit id via BuildConfig)
snapaie.admob.application.id=ca-app-pub-3940256099942544~3347511713
snapaie.admob.banner.unit.id=ca-app-pub-3940256099942544/6300978111
